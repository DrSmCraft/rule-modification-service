version: '2'
services:
  rule-modification-server:
    container_name: rule-modification-server
    build:
      context: .
      dockerfile: Dockerfile
    image: rule-modification-service:latest
    ports:
      - 9090:9090

